// Import the 'User' module from './User' file
const User = require('./User');

// Import the 'Task' module from './Task' file
const Task = require('./Task');

// Export an object that contains the imported 'User' and 'Task' modules
module.exports = { User, Task };
